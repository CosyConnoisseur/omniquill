<%= form_with(model: @character) do |form| %>
  <div>
    <%= form.label :document_upload, "Upload Character sheet" %>
    <%= form.file_field :document_upload, accept: 'image/png, image/jpeg, image/jpg, application/pdf' %>
  </div>

  <%= form.submit %>
<% end %>
