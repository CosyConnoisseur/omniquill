<div class="parchment-container mt-3 mx-auto headings">
<h2>Log in</h2>

<%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <div class="form-inputs">
    <%= f.input :email,
                required: false,
                autofocus: true,
                input_html: { autocomplete: "email" } %>
    <%= f.input :password,
                required: false,
                input_html: { autocomplete: "current-password" } %>
    <%= f.input :remember_me, as: :boolean if devise_mapping.rememberable? %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Log in", class: "btn btn-dark" %>
  </div>
<% end %>
<p class="mt-5 mb-0">New user?</p>
<%= render "devise/shared/links", class: "" %>
  <%= button_to "Continue as Guest", create_guest_session_path, method: :post, class: "btn btn-success btn-guest" %>
</div>
