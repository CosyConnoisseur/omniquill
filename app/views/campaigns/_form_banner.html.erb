<%= form_with(model: @campaign) do |form| %>
  <div>
    <%= form.label :banner, "Upload campaign banner" %>
    <%= form.file_field :banner, accept: 'image/png, image/jpeg, image/jpg, image/webp' %>
  </div>

  <%= form.submit %>
<% end %>
