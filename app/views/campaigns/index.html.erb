<div class="centering">
  <div class="medieval-title">
    <h1>Campaigns</h1>
  </div>
</div>
<div class="centering">
  <div class="bookshelf-container">
    <% @campaigns.each do |campaign| %>

        <div class="campaign-book" style="overflow: hidden;"> <%# something about this div was breaking the width of the page. %>
        <%= link_to campaign_path(campaign) do %>
          <% if campaign.card_image.attached? %>
            <%= image_tag campaign.card_image, class: "card-img-top img-fluid", style: "max-height: 550px;" %>
          <% else %>
            <%= image_tag "default-card-image.jpg", class: "card-img-top",  style: "max-height: 550px;" %>
          <% end %>
          <h5 class="book-title"><%= campaign.title %></h5>
        <% end %>
          <%= render "form_card_image", campaign:campaign %>
          <ul class="list-group-brown list-group-flush my-3">
            <li class="list-group-item">
              <strong>Players:</strong>

              <ul>
                <% campaign.participations.each do |participation| %>
                  <li>
                    <% if current_user.profile_picture.attached? %>
                      <%= image_tag current_user.profile_picture, class: "avatar", style: "height: 50px; width: 50px; object-fit: cover" %>
                    <% else %>
                      <%= image_tag "default-avatar.png", class: "avatar"%>
                    <% end %>
                    <%= participation.user.username %>
                    </li>
                <% end %>
              </ul>
            </li>
          </ul>

          <div class="text-center my-2">
            <%= link_to "Invite Player", join_campaign_url(campaign), class: "invite-button" %>
          </div>

        </div>
      </div>
    <% end %>
  </div>
</div>
<div class="centering my-3">
  <%= link_to new_campaign_path, class: "position-fixed", style: "bottom: 20px; right: 20px; z-index: 1050;" do %>
  <%= image_tag "campaign-creation-icon.png", class: "", style: "max-height: 100px; width: 100%; "%>
  <% end %>
</div>
<div class="centering">
  <div class="medieval-title">
    <h2>And characters!</h2>
  </div>
</div>
<div class="centering my-3">
  <% if current_user.participations.exists? %>
    <%= link_to "Create New Character", new_campaign_character_path(current_user.participations.last.campaign), class: "invite-button" %>
  <% end %>
</div>

<div class="list-div d-flex flex-column flex-md-row gap-4 justify-content-md-evenly align-items-center align-items-md-start m-3">

  <div class="flex-grow-1" style="max-width: 320px; min-width: 280px;">
    <h2 class="mb-3 text-center" style="color: white">Only My Characters!</h2>
    <ul class="list-unstyled d-flex flex-column gap-3">
      <% current_user.characters.each do |character| %>
        <li>
          <div>
            <%= link_to character_path(character), class: "text-decoration-none" do %>
              <div class="card shadow-sm" style="border-radius: 5px">
                <div class="card-body">
                  <div class="d-flex align-items-center justify-content-between">
                    <h5 class="card-title">
                      <%= character.name %>
                    </h5>
                    <% if character.portrait.attached? %>
                      <%= image_tag character.portrait, style: "width: 70px; height: 70px; object-fit: cover; border-radius: 8px;" %>
                    <% else %>
                      <%= image_tag "default-portrait.jpg", style: "max-height: 70px; border-radius: 8px;" %>
                    <% end %>
                  </div>
                    <p class="card-text text-muted">
                    Campaign: <%= character.participation.campaign.title %>
                  </p>
                  <p class="card-text text-muted">
                    Played by: <%= current_user.username ? current_user.username : current_user.email  %>
                      <% if character.participation.user.profile_picture.attached? %>
                        <%= image_tag current_user.profile_picture, class: "avatar", style: "height: 50px; width: 50px; object-fit: cover" %>
                      <% else %>
                        <%= image_tag "default-avatar.png", class: "avatar"%>
                      <% end %>
                  </p>
                </div>
              </div>
            <% end %>
          </div>
        </li>
      <% end %>
    </ul>
  </div>

  <div class="flex-grow-1" style="max-width: 400px; min-width: 280px;">
    <h2 class="mb-3" style="color: white">Every Character in the DB!</h2>
      <ul class="list-unstyled d-flex flex-column gap-3" style="max-width: 320px;">
        <% @characters.each do |character| %>
          <li>
            <div>
              <%= link_to character_path(character), class: "text-decoration-none" do %>
                <div class="card shadow-sm" style="border-radius: 5px">
                  <div class="card-body">
                    <div class="d-flex align-items-center justify-content-between">
                      <h5 class="card-title">
                        <%= character.name %>
                      </h5>
                      <% if character.portrait.attached? %>
                        <%= image_tag character.portrait, style: "width: 70px; height: 70px; object-fit: cover; border-radius: 8px;" %>
                      <% else %>
                        <%= image_tag "default-portrait.jpg", style: "max-height: 70px; border-radius: 8px;" %>
                      <% end %>
                    </div>
                    <p class="card-text text-muted">
                      Played by: <%= character.participation.user.username ? character.participation.user.username : character.participation.user.email  %>
                      <% if character.participation.user.profile_picture.attached? %>
                        <%= image_tag current_user.profile_picture, class: "avatar" , style: "height: 50px; width: 50px; object-fit: cover" %>
                      <% else %>
                        <%= image_tag "default-avatar.png", class: "avatar"%>
                      <% end %>
                    </p>
                  </div>
                </div>
              <% end %>
            </div>
          </li>
        <% end %>
      </ul>
  </div>
</div>
